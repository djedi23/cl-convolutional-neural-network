# This file is generated by GitLab CI
test prove:
  script:
  - git submodule update --init
  - ln -sfv $PWD/*.asd $HOME/quicklisp/local-projects/
  - sbcl --quit --eval "(ql:quickload 'prove)" --eval "(ql:quickload 'cl-cnn-test)" --eval "(prove:run :cl-cnn-test)" | tee /tmp/log && ! grep -q 'tests failed' /tmp/log
  tags:
  - sbcl
  except:
  - tags
